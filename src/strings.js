const name = "Patrick Gwinn";
const course = "Web Development 1";
const expectedGrade = 95;

const message =
	name + " is taking " + course + " and expects to earn " + expectedGrade + "%";
console.log(message);
const message2 = `${name} is taking ${course} and expects to earn ${expectedGrade}%`;
console.log(message2);
